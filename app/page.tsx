import { ComingSoon } from '@/components/ComingSoon';

// The full home page is parked at app/_home/page.tsx — see the restore
// instructions in lib/site.ts.
export default function HomePage() {
  return <ComingSoon />;
}
